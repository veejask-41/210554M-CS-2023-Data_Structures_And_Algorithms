#include <iostream>
using namespace std;

int main()
{
    int a, b, c; // defining variables as integer
    cin >> a >> b >> c; // getting inputs using stdin
    
    int total = a+b+c; // calculating total
    cout << total; // printing the total of a,b,c using stdout

    return 0;
}